# OS packages installed in the Konflux runtime image (Dockerfile.konflux
# final stage). The python CI job live-dnf installs this list except
# python3.12 / python3.12-pip (setup-python provides those).
#
# UBI packages come from requirements/ubi.repo (BaseOS, AppStream,
# CodeReady Builder). re2 and thrift are the EPEL 9 exception so AIPCC
# pyarrow==20.0.0 can load (libthrift-0.15.0.so and libre2). See
# requirements/epel.repo. snappy (libsnappy.so.1) is the CentOS Stream 9
# BaseOS exception; UBI omits it. See requirements/centos-stream.repo
# (includepkgs=snappy).
#
# Keep requirements/rpms.in.yaml packages in sync with this file.
# After changing it, regenerate:
#   rpm-lockfile-prototype --containerfile Dockerfile.konflux \
#     --outfile requirements/rpms.lock.yaml requirements/rpms.in.yaml

freetype
libgomp
libjpeg-turbo
libtiff
openblas-threads
openjpeg2
postgresql-libs
python3.12
python3.12-pip
re2
snappy
sqlite
thrift
utf8proc
